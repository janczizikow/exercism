export const encode = message => {
  return "";
};

export const decode = message => {
  // "12WB12W3B24WB"
  // let copy = message;
  let output = "";
  // while (copy.length) {
  //   const num = parseInt(copy);
  //   const letter = copy.match(/[a-z]/i);
  //   output += num ? letter[0].repeat(num) : letter[0];
  //   copy = copy.substring(letter.index);
  // }
  return output;
  // return num ? String.repeat() : output;
};
